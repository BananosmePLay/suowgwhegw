package neo;

import javax.annotation.Nullable;
import net.minecraft.util.ResourceLocation;

public class vK extends vI<Ii> {
   public vK(wC manager) {
      super(manager);
   }

   @Nullable
   protected ResourceLocation getEntityTexture(Ii entity) {
      return null;
   }

   // $FF: synthetic method
   // $FF: bridge method
   @Nullable
   protected ResourceLocation getEntityTexture(Ig var1) {
      return this.getEntityTexture((Ii)var1);
   }
}
