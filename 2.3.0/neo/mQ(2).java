package neo;

import net.minecraft.util.text.ITextComponent;

public interface mQ {
   void selectItem(mY var1);

   ITextComponent getSpectatorName();

   void renderIcon(float var1, int var2);

   boolean isEnabled();
}
