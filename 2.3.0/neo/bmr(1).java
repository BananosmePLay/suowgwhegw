package neo;

public class bmr extends kM implements bmx {
   private Bi option = null;

   public bmr(int id, int x, int y, Bi option) {
      super(id, x, y, option);
      this.option = option;
   }

   public Bi getOption() {
      return this.option;
   }
}
