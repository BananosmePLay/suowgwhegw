package neo;

public class z extends m {
   private final be item;

   public z(be item) {
      super(B.access$000());
      this.item = item;
   }

   public boolean test(Qy item) {
      return this.item.test(item);
   }
}
