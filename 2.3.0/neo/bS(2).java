package neo;

public class bS extends m {
   private final be item;

   public bS(be item) {
      super(bU.access$000());
      this.item = item;
   }

   public boolean test(Qy item) {
      return this.item.test(item);
   }
}
